% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vfa.R
\name{vfa_fit}
\alias{vfa_fit}
\title{Fit a separable VFA from (state, target) pairs via OLS}
\usage{
vfa_fit(targets, env, state_idx = seq(env$nSdx))
}
\arguments{
\item{targets}{Numeric vector of length \code{env$nSdx}; NA entries are
ignored (e.g. infeasible states).}

\item{env}{Environment produced by \code{dp_config}.}

\item{state_idx}{Integer vector of state indices that \code{targets}
corresponds to.  Default: all states.}
}
\value{
A \code{theta} parameter object.
}
\description{
Builds a one-hot indicator feature matrix \eqn{\Phi} (one indicator column
per location-level combination) and solves the least-squares problem
\eqn{\min_\theta \|\Phi\theta - y\|^2}.  The design matrix is rank-deficient
by one constant (all rows sum to \eqn{n_I + n_J}); \code{lm.fit} handles
this gracefully via QR pivoting — any NA coefficients are set to zero.
}
