% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_utils.R, R/utils.R
\name{create_gradient_palette}
\alias{create_gradient_palette}
\title{Create a Gradient Color Palette}
\usage{
create_gradient_palette(n, norm_ = NULL, low = "#80FFFF", high = "#FF80FF")

create_gradient_palette(n, norm_ = NULL, low = "#80FFFF", high = "#FF80FF")
}
\arguments{
\item{n}{Integer. The number of colors to generate in the gradient palette.}

\item{norm_}{Numeric vector (optional). A vector of normalized values to map to the gradient palette.}

\item{low}{Character. The hex code for the color representing the low end of the gradient. Default is \verb{#80FFFF}.}

\item{high}{Character. The hex code for the color representing the high end of the gradient. Default is \verb{#FF80FF}.}
}
\value{
A character vector of hex color codes. If \code{norm_} is provided, returns a vector of colors corresponding to \code{norm_}.
If \code{norm_} is not provided, returns the full gradient palette.

A vector of colors. If \code{norm_} is not provided, it returns a vector of \code{n} colors representing the gradient.
If \code{norm_} is provided, it returns a vector of colors corresponding to the mapped values.
}
\description{
This function generates a gradient color palette of \code{n} colors ranging from \code{low} to \code{high}.
If a vector of normalized values (\code{norm_}) is provided, the function maps these values
to colors in the gradient palette.

This function generates a gradient color palette between two colors, specified by \code{low} and \code{high}.
If \code{norm_} is provided, it maps the normalized z-values to corresponding colors in the gradient palette.
}
\examples{
# Generate a gradient of 5 colors
create_gradient_palette(5)

# Generate a gradient and map normalized values to colors
create_gradient_palette(5, norm_ = c(0.1, 0.5, 0.9))

create_gradient_palette(5)
create_gradient_palette(10, norm_ = runif(10))

}
